class Article {
    constructor(title, metaDescription, content, keywords, slug, tags) {
        this.title = title;
        this.metaDescription = metaDescription;
        this.content = content;
        this.keywords = keywords;
        this.slug = slug;
        this.tags = tags;
    }


}